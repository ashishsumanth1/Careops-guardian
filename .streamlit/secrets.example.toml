# Example secrets file for local testing.
# DO NOT COMMIT real secrets. Copy to `.streamlit/secrets.toml` locally and
# set OPENAI_API_KEY on Streamlit Community Cloud via the Secrets manager UI.

OPENAI_API_KEY = "sk-your-openai-key"
